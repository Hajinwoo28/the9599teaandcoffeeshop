# POS & Store Management System — Security-Enhanced Dependencies
# Group 5 | Course/Year: 20 | Date: 06/01/26
# Install: pip install -r requirements.txt

# --- Core Framework ---
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
requests==2.31.0
python-dotenv==1.0.0

# --- [FEATURE 1] CSRF Protection ---
Flask-WTF==1.2.1
WTForms==3.1.2

# --- [FEATURE 2] Rate Limiting ---
Flask-Limiter==3.7.0

# --- [FEATURE 3] HTTP Security Headers ---
Flask-Talisman==1.1.0

# --- [FEATURE 4] Authentication & Sessions ---
Flask-Login==0.6.3

# --- [FEATURE 5] Input Sanitization (XSS Prevention) ---
bleach==6.1.0

# --- [FEATURE 6] Password Hashing (PBKDF2-SHA256) ---
# Built into Werkzeug (Flask dependency) — no extra install needed

# --- [FEATURE 7] Two-Factor Authentication (TOTP) ---
pyotp==2.9.0
qrcode[pil]==7.4.2
Pillow==12.2.0

# --- [FEATURE 8] Secure Sessions / Signed Cookies ---
# Built into Flask via itsdangerous — no extra install needed

# --- Database ---
# For SQLite (development): built into Python — no extra install
# For PostgreSQL (production):
# psycopg2-binary==2.9.9

# --- Production WSGI Server (recommended) ---
# gunicorn==22.0.0
# waitress==3.0.0        # Windows alternative
